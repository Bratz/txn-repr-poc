# generated data — never commit
# NOTE: .gitignore does NOT support inline (end-of-line) comments — a pattern
# with trailing text matches nothing. Keep every comment on its own line.
*.parquet
*.csv
# keep the small reference sample
!data/pacs008_sample_500.csv
# regenerated by synth_pacs008.py; the committed reference is the .example file
data/column_schema.json
# v2 generated artifacts (data/synth_sequences.py)
data/column_schema_seq.json
# payment-twin generated artifacts (data/synth_workflow.py)
data/column_schema_twin.json
# India multi-rail generated artifacts (data/synth_india_rails.py)
data/column_schema_india.json

# model artifacts
*.pt
*.ckpt
party_store/
# run outputs
results.json
results_seq.json
results_twin.json
results_india.json
results_impute.json
scored.csv
# saved model checkpoints
ckpt/
# saved India serving model (serve_india.py) + sklearn probes
model_india/
*.joblib

# secrets — never commit SSH keys / tokens
sshkey
sshkey.pub
*.pem
id_rsa*
*.bundle

# scratch run logs
*.log

# python
__pycache__/
*.pyc
.venv/
venv/
